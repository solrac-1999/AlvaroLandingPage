// Mongoose connection stub - implement with actual MongoDB connection if needed
const connectMongo = async () => {
  console.log("MongoDB connection not configured - skipping");
  return Promise.resolve();
};

export default connectMongo;

